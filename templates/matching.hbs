<div class="matching component-inner">
  {{> component this}}
  <div class="matching__widget component-widget {{#unless _isEnabled}} disabled {{#if _isInteractionComplete}} complete submitted show-user-answer {{#if _isCorrect}}correct{{/if}} {{/if}} {{/unless}}">
    {{#each _items}}
    <div class="matching__item item {{#unless ../_isEnabled}}{{#if ../_canShowMarking}}{{#if _isCorrect}}correct{{else}}incorrect{{/if}}{{/if}}{{/unless}} item-{{@index}}">
      <div class="matching__item-title">{{{text}}}</div>
      <div class="matching__select-container component-item-color">
        <div class="matching__select-state">
          <div class="matching__select-icon component-text-color matching__select-icon--correct icon icon-tick"></div>
          <div class="matching__select-icon component-text-color matching__select-icon--incorrect icon icon-cross"></div>
        </div>
        {{!-- setting width as inline styling is recommended by select2 --}}
        <select class="matching__select component-text-color" style="width:100%">
          {{!-- an empty <option> element is needed by select2 to allow it to do its 'placeholder' functionality --}}
          <option></option>
          {{#each _options}}
          <option {{#if _isSelected}}selected{{/if}}>
            {{text}}
          </option>
          {{/each}}
        </select>
      </div>
    </div>
    {{/each}}
  </div>
  <div class="buttons">
  </div>
</div>
